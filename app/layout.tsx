import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Revioo — Build Trust. Get Discovered.",
  description:
    "The AI-powered review platform that helps businesses build authentic trust and get discovered by the customers who matter most.",
  keywords: ["reviews", "AI", "trust", "reputation", "business"],
  openGraph: {
    title: "Revioo — Build Trust. Get Discovered.",
    description:
      "AI-powered review management that transforms customer feedback into your biggest competitive advantage.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased min-h-screen`}
      >
        {children}
      </body>
    </html>
  );
}
